# Project Rules

- **Restore Point Command**: If the user tells you to go to "RESTORE POINT" (or says "RESTORE POINT"), immediately execute the shell command `git reset --hard HEAD` in the project root on their behalf using the terminal tools to restore the project files to the latest save point.
